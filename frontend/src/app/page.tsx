export default function Home() {
  // TODO (Sprint 3): chat UI, PDF upload for contract review (stretch goal),
  // wired to NEXT_PUBLIC_API_BASE_URL/ask (see architecture doc Section 2.4).
  return (
    <main style={{ fontFamily: "sans-serif", padding: "2rem" }}>
      <h1>Legal AI Copilot</h1>
      <p>Frontend scaffold — chat UI arrives in Sprint 3.</p>
    </main>
  );
}
